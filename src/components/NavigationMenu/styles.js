const styles = theme => ({
  Root: {
    backgroundColor: "#e1e1e1"
  },
  Tool: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    width: "80%",
    margin: "0 auto"
  }
});

export default styles;
